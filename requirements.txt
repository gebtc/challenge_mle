fastapi~=0.86.0
pydantic~=1.10.2
uvicorn~=0.15.0
numpy~=1.22.4
pandas~=1.3.5
scikit-learn>=1.3.0,<2.0.0

# Pin transitive dependencies for compatibility with Python 3.9 CI images.
# anyio>=4.0 removed start_blocking_portal used by starlette.testclient.
anyio<4.0.0
# starlette>=0.21.0 dropped support for the legacy TestClient transport API.
starlette<0.21.0
